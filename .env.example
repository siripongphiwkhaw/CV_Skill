# Postgres connection string (Vercel Postgres / Neon "Prisma" or pooled connection string).
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Generate with: npx auth secret   (or: openssl rand -base64 33)
AUTH_SECRET=""
